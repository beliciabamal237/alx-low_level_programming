#include <stdlib.h>
#include "main.h"

/**
 * _abs -Write a function that computes the absolute value of an integer.
 * @n: the int to be checked
 * Return: 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
		return (-n);
}
